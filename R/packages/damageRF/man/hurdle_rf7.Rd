% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdle_RF7.R
\name{hurdle_rf7}
\alias{hurdle_rf7}
\title{Title: Hurdle RF - Stage 2: Quantile Regression Forest}
\usage{
hurdle_rf7(
  identity,
  data,
  id,
  id_vars,
  target_vars,
  target,
  time_ax,
  row_limit = NA,
  trees = 1000,
  grid = 11,
  n_boots = 30,
  seed = NA,
  tune_metric = "mae",
  train_ratio = 0.8,
  n = 30,
  randomize_target = FALSE,
  test_run = FALSE,
  quantiles = c(0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95),
  train_ids = NULL,
  change_col = NULL
)
}
\arguments{
\item{identity}{Identity (bspid / group of bspids)}

\item{data}{A data.frame dataset}

\item{id}{Name of id variable}

\item{id_vars}{Character vector of id-like variables to exclude}

\item{target_vars}{Character vector of target-like variables to exclude}

\item{target}{Name of target variable}

\item{row_limit}{Optional: Limit number of rows in data}

\item{trees}{Optional: Number of trees}

\item{grid}{Optional: Tuning grid size}

\item{n_boots}{Optional: Number of bootstrap resamples}

\item{seed}{Optional: Seed}

\item{tune_metric}{Optional: "mae" or "rmse"}

\item{train_ratio}{Optional: Proportion of train data. Default: 0.80}

\item{n}{Optional: Lower limit of entries per target value}

\item{randomize_target}{Optional: Shuffle target for null model}

\item{quantiles}{Optional: Quantile levels to predict}

\item{train_ids}{Optional: character vector of bridge IDs (gsplitvar, i.e.
the part of id before the first "_") assigned to training. When provided
the internal splitting loop is skipped.}

\item{change_col}{Optional: name of a logical/binary column in data that is
TRUE (or 1) for rows where a change occurred. When provided training is
restricted to change observations only (hurdle filter). The column is
automatically excluded from predictors.}
}
\description{
Based on rf7. Accepts an external train_ids vector to enforce a
  shared train/test split across the hurdle model pipeline. When change_col
  is provided the training data is filtered to observations where a change
  actually occurred (hurdle filter), while the test data remains complete for
  fair evaluation. When train_ids and change_col are both NULL the function
  behaves identically to rf7.
}
