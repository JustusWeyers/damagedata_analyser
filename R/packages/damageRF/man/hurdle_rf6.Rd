% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdle_RF6.R
\name{hurdle_rf6}
\alias{hurdle_rf6}
\title{Title: Hurdle RF - Stage 1: Binary Classification}
\usage{
hurdle_rf6(
  identity = list(),
  data,
  id,
  id_vars,
  target_vars,
  target,
  time_ax,
  row_limit = NA,
  trees = 1000,
  grid = 11,
  n_boots = 30,
  seed = NA,
  tune_metric = "pr_auc",
  train_ratio = 0.8,
  n = 30,
  randomize_target = FALSE,
  test_run = FALSE,
  quantiles = NA,
  pred_threshold_mode = "standard",
  min_sensitivity = 0.95,
  train_ids = NULL
)
}
\arguments{
\item{target}{Name of targe variable}

\item{trees}{Optional: Number of trees}

\item{grid}{Optional: tuning grid}

\item{n_boots}{Optional: Number of resamples}

\item{seed}{Optional: seed}

\item{tune_metric}{Optional: Tuning metric "roc_auc", "pr_auc", "accuracy" "f_meas_weighted", etc.}

\item{n}{Optional: Number of rows to use}

\item{min_sensitivity}{Optional: Minimum sensitivity for minority class threshold selection (default 0.95)}

\item{train_ids}{Optional: character vector of bridge IDs (gsplitvar, i.e.
the part of id before the first "_") assigned to training. When provided
the internal splitting loop is skipped.}
}
\description{
Based on rf6_binary. Accepts an external train_ids vector to
  enforce a shared train/test split across the hurdle model pipeline.
  When train_ids is NULL the function behaves identically to rf6_binary.
}
